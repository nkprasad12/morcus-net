declare module "beta-code-js" {
  /**
   * Converts a string of Beta Code to Unicode.
   * @param betaCode The Beta Code string to convert.
   */
  export function betaCodeToGreek(betaCode: string): string;
}
