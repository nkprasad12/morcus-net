import { analyzeLatin } from "@/morceus/analyzer";

describe("analyzeLatin", () => {
  it("returns expected on single word", () => {
    // Just a placeholder test/
    expect(analyzeLatin("hello")).toEqual([]);
  });
});
